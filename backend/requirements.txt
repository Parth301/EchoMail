backend==0.2.4.1
Flask==2.3.3         # Flask 3.1.0 is the latest, but can have issues with some older libraries
Flask_Cors==3.0.10
Flask_JWT_Extended==4.7.1
pdfplumber==0.11.5
protobuf==3.20.1     # Downgraded to a version that is widely compatible with other libraries
python_docx==1.1.2
Werkzeug==2.2.3      # Compatible with Flask 2.3.3
mysql-connector-python==8.0.33
